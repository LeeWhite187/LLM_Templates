I've revised my spec template to a rev 3.
And created a set of migration instructions for how to modify a spec, written in prior versions of the spec template, up to current (template rev 3).

Each migration instruction steps a target spec up one template version.
Disregard any concerns or content in the Revision Log, as it will be deleted as part of the migration instructions.

Don't worry about what triggers a document revision increment in the title block.
We will increment the title block revision number at the end of this migration exercise.

There is no need for the methodology or spec template documents.
The migration instructions include everything we need for this exercise.

If needed, I've attached the rev 3 spec template.
But, the migration instruction should cover any details.